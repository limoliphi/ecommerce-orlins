{% extends 'base.html.twig' %}

{% block title %}Valider ma commande | La Boutique d'Orlins{% endblock %}

{% block content %}
    <h1>Je valide ma commande</h1>
    <p>Choisissez vos préférences avant de passer commande sur La Boutique d'Orlins.</p>
    <hr>
    <div class="row">
        <div class="col-md-6">
            {# me permet de récupérer les données de l'adresse et de les afficher grâce à public function __toString dans l'entité Address #}
            {# le formulaire cible une action en particulier en lui indiquant la route de la méthode qui nous intéresse #}
            {% set formHtml %}
                {{ form_start(form, {action:path('order_summary')}) }}
                {{ form_label(form.addresses, 'Choisissez votre adresse de livraison') }}
                <a href="{{ path('add_account_address') }}">Ajoutez une nouvelle adresse</a>
                {{ form_end(form) }}
            {% endset %}
            {{ formHtml|replace({'[br]' : '<br/>'})|raw }}
        </div>
        <div class="col-md-6">
            <div class="text-center">
                <b>Votre commande</b><br/>
                <p>Retrouver le récapitulatif de vos produits</p><br/>
            </div>
            <div class="order-summary">
                {% for key,product in cart %}
                    <div class="row {% if key > 0 %}mt-3{% endif %}">
                        <div class="col-2">
                            <img src="/uploads/{{ product.product.illustration }}" alt="{{ product.product.name }}"
                                 height="75px">
                        </div>
                        <div class="col-8 my-auto">
                            {{ product.product.name }}<br>
                            <small>{{ product.product.subtitle }}</small>
                        </div>
                        <div class="col-2 my-auto">
                            x {{ product.quantity }}
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
