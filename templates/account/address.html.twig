{% extends 'base.html.twig' %}

{% block title %}Mes adresses | La Boutique d'Orlins{% endblock %}

{% block content %}
    <a href="{{ path('add_account_address') }}" class="btn btn-info btn-sm float-right">Ajouter une adresse</a>
    <h1>Mes adresses</h1>
    <p>
        C'est dans cet espace que vous allez pouvoir gérer toutes vos adresses.
    </p>
    <hr>
    <p>
        <a href="{{ path('account') }}">Retour</a>
    </p>
    <hr>
    {% if app.user.addresses|length == 0 %}
        <p class="text-muted text-center mt-5">Vous n'avez pas encore saisi d'adresse dans votre compte client. Pour y
            remédier<a href="{{ path('add_account_address') }}"> cliquez ici</a>.
        </p>
    {% else %}
        <div class="row">
            {% for address in app.user.addresses %}
                <div class="col-md-4">
                    <div class="card" style="width: 18rem;">
                        <div class="card-body">
                            <h5 class="card-title">{{ address.name }}</h5>
                            <p class="card-text">
                                {{ address.address }}<br>
                                {{ address.postal }} {{ address.city }} - {{ address.country }}
                            </p>
                            <a href="{{ path('edit_account_address', {'id': address.id }) }}">Modifier</a> | <a href="{{ path('delete_account_address', {'id': address.id }) }}">Supprimer</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
{% endblock %}
